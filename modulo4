let str1=("abcdefghijka")
console.log(str1.indexOf("g"))
console.log(str1.charAt("6"))
console.log(str1.match(/a/g))
console.log(str1.includes("a"))
console.log(str1.startsWith("a"))
let str=("santiago")
let str2=("fracassi")
let estudiante=str.concat(" ",str2)
let estudianteMayus=estudiante.toUpperCase()
let estudianteMinus=estudiante.toLowerCase()
let numeroletras=estudiante.length
let primeraletra=estudiante.startsWith()
let ultimaletra=estudiante.endsWith()
function eliminarEspacios(str) 
{
    return str.split(" ").join("");
}
  let estudiantesinespacios = eliminarEspacios(estudiante);
  let varboolean=estudiante.includes(str)
  
